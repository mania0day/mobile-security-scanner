# objection (placeholder — not implemented)

This service directory is scaffolding for potential future Frida/objection-based
dynamic analysis. It is **not** referenced anywhere in `compose.yaml` or
`backend/orchestrator/pipeline.py`, its `Dockerfile`/`requirements.txt` are
empty, and it does not build or run.

Left in place intentionally rather than deleted, since the scaffolding may be
a starting point for future work — if you're picking this up, treat it as an
empty shell, not an in-progress feature.
